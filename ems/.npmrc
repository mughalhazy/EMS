cache=D:\npm-cache
prefer-offline=false
fund=false
audit=false

# Workspace-local output paths (relative)
# These mirror the .workspace/ structure for tools that support local path config
# Actual runtime paths for npm cache/temp are on D:\ (see above)

