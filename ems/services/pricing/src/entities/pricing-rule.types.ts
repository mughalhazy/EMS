export enum PriceRuleType {
  BasePrice = 'base_price',
  DynamicAdjustment = 'dynamic_adjustment',
  ScheduleOverride = 'schedule_override',
}

export enum DiscountType {
  Percentage = 'percentage',
  FixedAmount = 'fixed_amount',
}
