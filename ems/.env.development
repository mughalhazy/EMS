# EMS runtime environment
APP_ENV=development
NODE_ENV=development

# Auth
JWT_SECRET=local-dev-secret-change-me

# OpenSearch
OPENSEARCH_NODE=http://localhost:9200
OPENSEARCH_TLS_REJECT_UNAUTHORIZED=false
OPENSEARCH_USERNAME=
OPENSEARCH_PASSWORD=
EVENT_SEARCH_INDEX_NAME=events_dev
ATTENDEE_DIRECTORY_SEARCH_INDEX_NAME=attendee_directory_dev

# Commerce
STRIPE_COMPAT_BASE_URL=
STRIPE_COMPAT_API_KEY=
